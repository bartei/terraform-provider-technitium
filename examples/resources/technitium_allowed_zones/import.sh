# Import an existing set of allowed zone entries by listing the domains
# this resource should manage (comma-separated).
terraform import technitium_allowed_zones.example "good.example.com,cdn.example.net"
