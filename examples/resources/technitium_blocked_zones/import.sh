# Import an existing set of blocked zone entries by listing the domains
# this resource should manage (comma-separated).
terraform import technitium_blocked_zones.example "ads.example.com,tracker.example.net"
